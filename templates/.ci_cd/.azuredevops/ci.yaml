---
trigger:
  - none

resources:
  repositories:
    - repository: templates
      type: git
      name: ${project_name}/${repository_name_templates}

lockBehavior: sequential

extends:
  template: .templates/ci.yaml@templates
